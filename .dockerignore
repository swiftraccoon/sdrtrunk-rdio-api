# Send only declared build inputs to the daemon/remote builder. An allowlist
# prevents an unrecognized local secret, database, recording, or editor file
# from entering the build context at all.
**

!Dockerfile
!.dockerignore
!pyproject.toml
!uv.lock
!README.md
!LICENSE
!cli.py

!src/
!src/**

!config/
!config/config.example.yaml
